console.log(7 / 0);  // Atribui um valor infinito.
console.log("10" / 2); // Mesmo o valor entre aspas sendo uma string o javascript verifica e ver pode realizar a operação.
console.log("Show!" * 2);  //Informa que isto Não é um número "NaN"
console.log(0.1 + 0.7);  //Não apresentará o valor real, por se tratar do tipo de operação que o javaScript suporta.
//console.log(10.toString());  // Não consegue desta forma transformar para String.
console.log((10).toFixed(2));  // Desta forma consegue.
