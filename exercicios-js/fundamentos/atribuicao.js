const a = 7
let b = 3

b += a  // é o mesmo que b = b + a
console.log(b);

b -= 4  // é o mesmo que b = b -4
console.log(b);

b *= 2  // é o mesmo que b = b *2
console.log(b);

b /= 2  // é o mesmo que b = b / 2
console.log(b);

b %= 2  // é o mesmo que b = b % 2  >> se o resultado de "B" for PAR a saída é 0(zero), se for IMPAR então a saída é 1
console.log(b);

